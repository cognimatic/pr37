import { ReactElement } from 'react';
import { Link as LinkStorybook } from '@storybook/components';
export interface Link extends LinkStorybook {
    active: boolean;
    title: string;
}
declare type ExpandedThemeValue = {
    title: string;
};
export declare type ThemeValue = string | ExpandedThemeValue;
export declare const Tool: () => ReactElement;
export {};
